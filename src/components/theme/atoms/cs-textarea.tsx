import TextArea from "antd/es/input/TextArea";
import { TextAreaProps } from "antd/lib/input";

const CSTextarea = (props: TextAreaProps) => {
  return <TextArea {...props} />;
};

export default CSTextarea;
