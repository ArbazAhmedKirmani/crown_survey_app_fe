import { Typography } from "antd";

const SignupScreen = () => {
  return (
    <div>
      <Typography.Title level={4}>This is a Signup Screen</Typography.Title>
      <Typography.Title level={5}>
        You can add your desired screen field here.
      </Typography.Title>
    </div>
  );
};

export default SignupScreen;
